import React, { useState } from 'react'
import { View, ScrollView, Text } from "react-native"

function ChatMessages() {
  return (<>
    <ScrollView style={{ backgroundColor: "#e1f7da", borderRadius: "30%", padding: "5%" }}>
      <Text>
        deneme
      </Text>
    </ScrollView>

  </>
  )
}

export default ChatMessages